#!/usr/bin/node

const x = Math.floor(Number(process.argv[2]));
if (x === NaN) {
    console.log('Missing number of occurrences');
}
else{
    for (let i = 0; index < x; i++) {
        console.log('C is fun');
    }
}